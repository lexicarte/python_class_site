import 'package:flutter/material.dart';
import 'package:python_class_site/features/widgets/topic_screen.dart';

class WorkingWithData extends StatelessWidget {
  const WorkingWithData({super.key});

  @override
  Widget build(BuildContext context) {
    String title = 'Working with Data';

    return TopicScreen(
      title: title,
      body: Column(
        children: [],
      ),
    );
  }
}
